services:
  lspd-warrant:
    build: .
    container_name: lspd-warrant
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - .env.local
    volumes:
      - warrant-data:/app/data

volumes:
  warrant-data:
